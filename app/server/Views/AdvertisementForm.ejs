<!DOCTYPE html>
<html lang="en">
<head>
  <meta charset="UTF-8">
  <title>Create Advertisement</title>
  <link
    href="https://cdn.jsdelivr.net/npm/bootstrap@5.3.3/dist/css/bootstrap.min.css"
    rel="stylesheet"
  >
</head>
<body class="bg-light">
  <div class="container mt-5">
    <h2 class="mb-4">Create Advertisement</h2>
    <form action="/advertisement/<%= carId %>" method="POST" class="card p-4 shadow-sm">
      
      <div class="mb-3">
        <label for="carId" class="form-label">Car ID</label>
        <input type="number" class="form-control" id="carId" name="carId" value="<%= carId %>" readonly>
      </div>

      <div class="mb-3">
        <label for="grabber" class="form-label">Car Attention Grabber</label>
        <textarea class="form-control" id="grabber" name="carAttentionGrabber" rows="3"
          maxlength="300" required></textarea>
      </div>

      <div class="form-check mb-2">
        <input class="form-check-input" type="checkbox" id="financeAvailable"
          name="financeAvailable">
        <label class="form-check-label" for="financeAvailable">
          Finance Available
        </label>
      </div>

      <div class="form-check mb-4">
        <input class="form-check-input" type="checkbox" id="discounted" name="discounted">
        <label class="form-check-label" for="discounted">
          Discounted
        </label>
      </div>

      <button type="submit" class="btn btn-primary w-100">Create Advertisement</button>
    </form>
  </div>

  <script
    src="https://cdn.jsdelivr.net/npm/bootstrap@5.3.3/dist/js/bootstrap.bundle.min.js"
  ></script>
</body>
</html>
