<!DOCTYPE html>
<html lang="en">
<head>
  <meta charset="UTF-8">
  <title><%= car.CarTitle %> - Details</title>
  <style>
    body { font-family: 'Segoe UI'; margin: 40px; background: #f9f9f9; }
    .card {
      max-width: 600px;
      margin: auto;
      background: white;
      border-radius: 10px;
      box-shadow: 0 0 10px rgba(0,0,0,0.1);
      padding: 30px;
    }
    .card h1 { margin-top: 0; color: #007bff; }
    .card p { margin: 10px 0; }
    .back { margin-top: 20px; display: inline-block; color: #007bff; }
  </style>
</head>
<body>
  <div class="card">
    <h1><%= car.CarTitle %></h1>
    <p><strong>Price:</strong> $<%= car.CarPrice.toLocaleString() %></p>
    <p><strong>Year:</strong> <%= car.ManufactureYear %></p>
    <p><strong>Type:</strong> <%= car.BodyType %></p>
    <p><strong>Engine:</strong> <%= car.EngineVolume %> L</p>
    <p><strong>Transmission:</strong> <%= car.TransmissionType %></p>
    <p><strong>Mileage:</strong> <%= car.Mileage %> km</p>
    <a href="/" class="back">← Back to Listings</a>
  </div>
</body>
</html>
